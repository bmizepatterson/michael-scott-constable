{
  "name": "michael-scott-constable",
  "description": "Michael Scott for Fayette County Constable, District 2",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "browser-sync": "./node_modules/.bin/browser-sync start --server 'docs' --files 'docs'",
    "sass": "./node_modules/.bin/sass --watch src/sass/main.scss docs/css/main.css",
    "copy": "./node_modules/.bin/cpx \"src/**/*.html\" docs --watch",
    "dev": "./node_modules/.bin/concurrently \"npm run browser-sync\" \"npm run sass\" \"npm run copy\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bmizepatterson/michael-scott-constable.git"
  },
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/bmizepatterson/michael-scott-constable/issues"
  },
  "homepage": "https://bmizepatterson.github.io/michael-scott-constable/",
  "devDependencies": {
    "bootstrap": "^4.1.3",
    "browser-sync": "^2.24.7",
    "cpx": "^1.5.0",
    "sass": "^1.14.0"
  },
  "dependencies": {
    "concurrently": "^4.0.1"
  }
}
